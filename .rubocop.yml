inherit_gem:
  ncs_rubocop_conf:
    - config/base.yml
    - config/rake.yml
    - config/rspec.yml
    - config/native_extension.yml

# RuboCop rationale: Marshal is the documented trusted-input persistence format.
Security/MarshalLoad:
  Exclude:
    - lib/tsp_kit/marshal_support.rb
